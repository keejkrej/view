{
  "name": "view",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.22",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run build:deps && bun run --cwd apps/tauri package:release",
    "build:deps": "bun run --cwd packages/core-ts build && bun run --cwd packages/align build && bun run --cwd packages/annotate build",
    "dev": "bun run --cwd apps/tauri tauri dev",
    "test": "bun run --cwd packages/core-ts test && bun run --cwd packages/align test && bun run --cwd packages/annotate test",
    "typecheck": "bun run --cwd packages/core-ts typecheck && bun run --cwd packages/align typecheck && bun run --cwd packages/annotate typecheck && bun run --cwd apps/tauri typecheck"
  }
}
