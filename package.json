{
  "name": "view",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.22",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run scripts/run-workspaces.mjs build --scope packages && bun run --cwd apps/desktop build",
    "dev": "bun run --cwd apps/desktop dev",
    "release": "bun run --cwd apps/desktop release",
    "test": "bun run scripts/run-workspaces.mjs test --scope packages",
    "typecheck": "bun run scripts/run-workspaces.mjs typecheck --scope packages && bun run --cwd apps/desktop typecheck && cargo check --workspace"
  }
}
